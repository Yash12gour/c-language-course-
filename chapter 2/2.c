#include <stdio.h>

int main (){

	int a,b;
	int c = a+b;

	printf("the value of a is \n");
	scanf("%d", &a);

	printf("the value of b is \n");
	scanf("%d", &b);

	printf("the value of c is %d\n", a+b);




	//  %modulas is used for finding  remainder
	printf("the remainder when a is divided by b is= %d\n", a%b);



	return 0;
}
