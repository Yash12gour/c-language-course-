#include <stdio.h>

int main() {
int r= 6;
int h= 10;
printf("the area of circle with radius %d is %f\n", r, 3.14*r*r);
   printf("the volume of circle with radius %d and hieght %d is %f", r, h, 3.14*r*r*h); 
    return 0;
}

