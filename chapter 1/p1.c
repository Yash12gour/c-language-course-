#include <stdio.h>

int main() {
//	int length=3;
//	int width=6;
int length,width;
printf("enter the value of length\n");
scanf("%d", &length);
printf("enter the value of width\n");
scanf("%d", &width);

    printf("the area of reactangle is %d", length*width);
    return 0;
}

