#include<stdio.h>
int main(){
    int marks[90]; // reserve space to store 90 integers

     marks[0]=45;
        marks[2]=67;
        // we can go all the way till 89
        printf(" marks 0 and marks 2 are %d and %d", marks[0],marks[2]);
return 0;
}
