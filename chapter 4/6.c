#include<stdio.h>
int main(){
int n;
    printf("the value of n \n");
    scanf("%d", &n);
	for(int i =0; i<n;i++){

            if(i==10){
               // break; // it means to exit the loop immediately
                continue; // it means to ski[p the term and continue the loop

	}  printf("%d\n",i);
            }

printf("the loop has been completed");
	return 0;
}
